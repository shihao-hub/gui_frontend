"""

### 子目录为父目录服务（A → B）：子目录是父目录的“插件”或“扩展”。
```txt
core/           # 父目录 A（核心框架）
└── plugins/    # 子目录 B（插件）
    └── __init__.py
```

### 总结
- 优先单向依赖：根据模块职责决定方向，通常子目录调用父目录（B → A）。
- 特殊场景反向调用：若父目录需调用子目录（A → B），必须通过抽象（如接口、事件驱动）解耦。
- 核心原则：高层模块可以依赖底层模块，但底层模块不应依赖高层模块（依赖倒置原则）。

"""
